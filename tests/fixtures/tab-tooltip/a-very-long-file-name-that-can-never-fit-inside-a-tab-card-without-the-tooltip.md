# Long file name

This file exists so the tab strip has a title that can never fit inside the
tab card. Its whole name belongs on the dwell tooltip instead.

## Companion content

| tab | occupies | width |
| --- | --- | --- |
| tooltip | card | below |
- a **mixed** *list* with `code` and a [link](tab-tooltip.md)